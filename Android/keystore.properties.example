# Copy this file to `keystore.properties` for local release signing.
# Never commit `keystore.properties` or your keystore to VCS.

storeFile=your-release.keystore
storePassword=********
keyAlias=********
keyPassword=********

